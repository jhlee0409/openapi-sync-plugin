---
description: Explain FSD concepts with project-specific examples
---

# /fsdarch:explain

FSD 개념을 프로젝트 맥락에서 설명합니다. 학습 곡선을 낮추고 팀 내 이해도를 통일합니다.

## Syntax

```
/fsdarch:explain <topic>
```

## Available Topics

| Topic | Description |
|-------|-------------|
| `layers` | 7개 레이어와 각각의 역할 |
| `feature-vs-widget` | Feature와 Widget의 차이 |
| `entity-vs-feature` | Entity와 Feature의 차이 |
| `public-api` | Public API 패턴과 이유 |
| `segments` | ui, model, api, lib 세그먼트 |
| `cross-imports` | Cross-import 금지 규칙 |
| `hierarchy` | 레이어 계층 규칙 |
| `shared` | Shared 레이어 사용법 |
| `slices` | 슬라이스 분리 기준 |
| `<custom>` | 자유 질문 (FSD 관련) |

## Execution Flow

### Step 1: Parse Topic

1. 미리 정의된 토픽인지 확인
2. 아니면 자유 질문으로 처리

### Step 2: Load Project Context

1. `.fsd-architect.json` 로드 (있으면)
2. 현재 프로젝트 구조 분석
3. 실제 예시 준비

### Step 3: Generate Explanation

1. 개념 설명 (이론)
2. 프로젝트 예시 (실제)
3. 관련 규칙
4. 추가 학습 자료

### Step 4: Display Result

토픽별 맞춤형 출력.

## Examples

### Example 1: Feature vs Widget

```
/fsdarch:explain feature-vs-widget
```

```
═══════════════════════════════════════════════════════════════
                  FEATURE vs WIDGET
═══════════════════════════════════════════════════════════════

📚 핵심 개념

Feature (기능)
  → 사용자 시나리오 단위의 기능
  → "사용자가 ~할 수 있다"로 설명 가능
  → 비즈니스 로직 포함 가능
  → 다른 Feature와 독립적

Widget (위젯)
  → 여러 Feature를 조합한 UI 블록
  → 자체 비즈니스 로직 최소화
  → 페이지에서 재사용되는 "덩어리"

───────────────────────────────────────────────────────────────

🎯 결정 기준

"이것이 독립적인 사용자 행동인가?"
  → YES: Feature (예: "로그인", "장바구니 추가", "리뷰 작성")
  → NO: Widget (예: "헤더", "사이드바", "제품 카드 그리드")

"다른 기능과 조합되어야 하는가?"
  → YES: Widget (예: HeaderWidget = auth + cart + search 조합)
  → NO: Feature

───────────────────────────────────────────────────────────────

📁 당신의 프로젝트 예시

Features (현재 8개):
  ├── auth/          → 사용자가 "로그인할 수 있다"
  ├── cart/          → 사용자가 "장바구니를 관리할 수 있다"
  ├── checkout/      → 사용자가 "결제할 수 있다"
  ├── favorites/     → 사용자가 "찜 목록을 관리할 수 있다"
  └── ...

Widgets (현재 3개):
  ├── header/        → auth + cart + search 조합
  ├── product-card/  → product entity + favorites feature 조합
  └── sidebar/       → navigation + user-info 조합

───────────────────────────────────────────────────────────────

⚠️ 흔한 실수

1. Feature가 너무 큼
   ✗ features/user/ (너무 포괄적)
   ✓ features/user-profile/, features/user-settings/

2. Widget에 비즈니스 로직
   ✗ widgets/header/model/authLogic.ts
   ✓ features/auth/ 에서 로직, widget은 조합만

3. Feature 간 의존성
   ✗ features/auth → features/cart
   ✓ widgets/auth-cart 에서 조합

───────────────────────────────────────────────────────────────

📚 더 알아보기

• https://feature-sliced.design/docs/reference/layers#features
• https://feature-sliced.design/docs/reference/layers#widgets

═══════════════════════════════════════════════════════════════
```

### Example 2: Public API

```
/fsdarch:explain public-api
```

```
═══════════════════════════════════════════════════════════════
                     PUBLIC API
═══════════════════════════════════════════════════════════════

📚 핵심 개념

Public API = 슬라이스의 "정문"

모든 외부 접근은 index.ts를 통해서만 가능해야 합니다.
내부 구조 변경이 외부에 영향을 주지 않도록 캡슐화합니다.

───────────────────────────────────────────────────────────────

🏗️ 구조

src/entities/user/
├── index.ts          ← PUBLIC API (정문)
├── model/
│   ├── index.ts
│   ├── types.ts      ← 내부
│   └── store.ts      ← 내부
├── api/
│   └── userApi.ts    ← 내부
└── ui/
    └── UserCard.tsx  ← 내부

───────────────────────────────────────────────────────────────

✅ 올바른 사용

// src/features/auth/model/useAuth.ts
import { User, getUserById } from '@entities/user';  // ← index.ts에서

// src/entities/user/index.ts
export type { User } from './model/types';
export { getUserById } from './api/userApi';
export { UserCard } from './ui/UserCard';

───────────────────────────────────────────────────────────────

❌ 잘못된 사용 (Public API 우회)

// src/features/auth/model/useAuth.ts
import { User } from '@entities/user/model/types';     // ✗ 내부 접근
import { getUserById } from '@entities/user/api/userApi'; // ✗ 내부 접근

───────────────────────────────────────────────────────────────

🎯 왜 중요한가?

1. 캡슐화
   → 내부 구조 변경이 자유로움
   → types.ts를 dto.ts로 바꿔도 외부 영향 없음

2. 의존성 명확화
   → 무엇이 외부에 노출되는지 한눈에
   → 순환 참조 방지

3. 리팩토링 용이
   → Public API만 유지하면 내부는 자유롭게

───────────────────────────────────────────────────────────────

📁 당신의 프로젝트 상태

Public API 준수율: 95%

잘 되어 있음:
  ✓ entities/user/index.ts
  ✓ entities/product/index.ts
  ✓ features/auth/index.ts

개선 필요:
  ⚠ entities/product/lib/formatters.ts
    → 3곳에서 직접 import 중
    → index.ts에 export 추가 권장

───────────────────────────────────────────────────────────────

📚 더 알아보기

• https://feature-sliced.design/docs/reference/public-api
• /fsdarch:validate 로 위반 사항 확인

═══════════════════════════════════════════════════════════════
```

### Example 3: Custom Question

```
/fsdarch:explain "entities에서 다른 entity를 참조해도 되나요?"
```

```
═══════════════════════════════════════════════════════════════
           ENTITY 간 참조 규칙
═══════════════════════════════════════════════════════════════

📚 답변

기본 규칙: Entity 간 직접 import는 금지입니다.

하지만 FSD 2.0의 @x 크로스 레퍼런스를 사용하면 가능합니다.

───────────────────────────────────────────────────────────────

⚠️ 기본: 금지

// src/entities/order/model/types.ts
import { User } from '@entities/user';    // ✗ 금지
import { Product } from '@entities/product'; // ✗ 금지

export interface Order {
  user: User;      // Entity 간 의존성 발생
  products: Product[];
}

───────────────────────────────────────────────────────────────

✅ 해결책 1: @x 크로스 레퍼런스 (FSD 2.0+)

src/entities/order/
├── @x/
│   ├── user.ts      // user entity와의 계약
│   └── product.ts   // product entity와의 계약
├── model/
│   └── types.ts
└── index.ts

// src/entities/order/@x/user.ts
export type { User } from '@entities/user';

// src/entities/order/model/types.ts
import { User } from '../@x/user';
import { Product } from '../@x/product';

───────────────────────────────────────────────────────────────

✅ 해결책 2: ID만 참조

// src/entities/order/model/types.ts
export interface Order {
  userId: string;      // ID만 저장
  productIds: string[]; // ID만 저장
}

// 실제 조합은 features 또는 widgets에서
// src/features/order-details/model/useOrderWithDetails.ts
import { Order } from '@entities/order';
import { User, getUserById } from '@entities/user';
import { Product, getProductById } from '@entities/product';

───────────────────────────────────────────────────────────────

💡 당신의 프로젝트에서

현재 entity 간 참조:
  • entities/order → entities/user (ID 참조 ✓)
  • entities/order → entities/product (직접 참조 ⚠)

권장: entities/order/@x/product.ts 추가

───────────────────────────────────────────────────────────────

📚 더 알아보기

• https://feature-sliced.design/blog/ddd-for-frontend-devs
• https://feature-sliced.design/docs/reference/layers#entities

═══════════════════════════════════════════════════════════════
```

## Flags

| Flag | Description | Example |
|------|-------------|---------|
| `--brief` | 간단한 설명만 | `/fsdarch:explain layers --brief` |
| `--no-examples` | 프로젝트 예시 생략 | `/fsdarch:explain public-api --no-examples` |
| `--ko` | 한국어 출력 (기본) | `/fsdarch:explain layers --ko` |
| `--en` | 영어 출력 | `/fsdarch:explain layers --en` |

## Error Handling

### E107: Unknown Topic

```
[E107] Unknown topic

Topic 'controllers' not recognized.

Available topics:
  layers, feature-vs-widget, entity-vs-feature,
  public-api, segments, cross-imports, hierarchy,
  shared, slices

Or ask a custom question:
  /fsdarch:explain "your question here"
```
